 using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EndLevel : MonoBehaviour
{
    public GameManager gameManager;

    public int numOfPowerups;
    public int numOfCandies;
    public bool timePassed= false;

    // Start is called before the first frame update
    void Start()
    {
     //   numOfPowerups = GlobalAchievemetns.ach03Count;
     //   numOfCandies = GlobalAchievemetns.ach01Count;
    }

    // Update is called once per frame
    void Update()
    {
        
        
  //      if (GlobalAchievemetns.triggerAch02 == true && numOfCandies == 10 && numOfPowerups ==1)
  //      {
  //          timePassed = true;
  //          gameManager.CompleteLevel();
        }

       
            //gameManager.CompleteLevel();
    
  //  }
}
